pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo "Build started..."'
            }
        }

        stage('Test') {
            steps {
                sh 'echo "Testing..."'
            }
        }
    }
}
